[project]
name = "fix"
version = "0.1.0"
description = "Watch GitHub pull requests and launch Codex sessions for CI failures and reviews."
readme = "README.md"
requires-python = ">=3.9"
license = { file = "LICENSE" }
authors = [{ name = "Taylor Steinberg" }]
dependencies = []

[project.scripts]
fix = "fix:main"

[build-system]
requires = ["uv_build>=0.10.4,<0.11.0"]
build-backend = "uv_build"
